<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Sampled from the icon's upper sky region so the Android 12+ splash
         backdrop (visible in the launcher-mask corners while the animated
         icon is drawn) blends into the artwork instead of looking like a
         hard dark frame. The launcher icon itself no longer uses this color
         at all — the art now fills the entire adaptive-icon background. -->
    <color name="launch_bg">#62888E</color>
</resources>
